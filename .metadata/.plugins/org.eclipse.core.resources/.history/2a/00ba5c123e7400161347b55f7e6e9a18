
public class DoMath {
	public static void mant(String[] args){
		Calculate.square(5);
		Calculate.cube(number);
		Calculate.average(inputnumber, otherinput, inputofinputs);
		Calculate.average(inputnumber, otherinput, inputofinputs);
		Calculate.toDegrees(input);
		Calculate.toRadians(input);
		Calculate.discriminant(0, 0, 0);
		Calculate.toImproperFrac(cookie, are, cool);
		Calculate.MixedNum(0, 0);
		Calculate.foil(a, b, c, n);
		Calculate.isDivisibleBy(DivInput, Divinputt);
		Calculate.absValue(number);
		Calculate.max(Trump, HRC);
		Calculate.max();
		Calculate.min(Trump, HRC);
		
	}
}
